# DISABLED — this workflow was overwriting production UI on every push.
# Kept for history only. Do not re-enable without review.
name: Margots UI refinement (disabled)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "UI auto-inject is disabled. Edit docs/index.html directly."
