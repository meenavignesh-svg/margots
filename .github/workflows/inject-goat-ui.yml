# DISABLED — prevented stable production UI.
name: Apply Margots UI (disabled)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Layout injection disabled. Canonical UI is docs/index.html."
