{
  "name": "margots",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-agent molecular intelligence — browser-native bioinformatics workspace",
  "scripts": {
    "test": "node docs/tests/bio-core.test.js",
    "test:backend": "python -m pytest -q tests",
    "check:python": "python -m compileall app.py backend.py core tools",
    "serve": "python3 -m http.server 8080 --directory docs",
    "serve:api": "python backend.py"
  },
  "engines": {"node": ">=18"},
  "license": "MIT"
}
